{
  "name": "yt-frame-extractor",
  "version": "1.0.0",
  "description": "Extract frames from YouTube videos",
  "scripts": {
    "dev": "vercel dev",
    "build": "mkdir -p public && cp -r frontend/* public/"
  },
  "dependencies": {
    "ffmpeg-static": "^5.2.0",
    "ytdl-core": "^4.11.5"
  },
  "devDependencies": {
    "vercel": "^54.6.1"
  }
}
